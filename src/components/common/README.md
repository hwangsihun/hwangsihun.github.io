# Common Components

이 폴더는 공통 마크업과 공통 동작을 모으는 용도입니다.

추천 기준:

- `button/`: 공통 버튼, 아이콘 버튼, 더보기 버튼
- `section-header/`: 타이틀 + 액션 버튼 조합
- `card/`: 뉴스 카드, 공지 카드, 배너 카드
- `nav/`: 상단 메뉴, 모바일 헤더

JSP 연동 시에는 이 폴더 단위를 기준으로 `include` 또는 `tag file`로 옮기면 관리가 쉬워집니다.
